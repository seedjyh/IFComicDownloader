lua脚本设计原则：
每个站点都必须实现的函数：
    IsMySite，输入一个URL，输出一个bool
        用于判断这个URL是否是这个站点相关的。
    DownloadAndAnapysePage，输入一个URL，输出一个XML。
        这个函数会分析URL，判断出是哪类page，从而在调用时选择正确的分析函数。
        这个函数会调用外部提供的C++静态方法进行下载。
        对于页面里出现的链接，函数会进行判断：如果是子页面的url，则将url保存到分析结果中；如果是当前页面的jsp等，则将url保存到